<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>RestaurantAdviser</title>
        <link rel="stylesheet" href="css/styles.css">
        <style>
            body {
                font-family: Arial, sans-serif;
                margin: 20px;
                padding: 20px;
                background-color: #f9f9f9;
            }
            header {
                font-size: 24px;
                font-weight: bold;
                margin-bottom: 20px;
            }
            .restaurant-list {
                list-style-type: none;
                padding: 0;
            }
            .restaurant-item {
                background: white;
                padding: 15px;
                margin: 10px 0;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }
            .restaurant-item h2 {
                font-size: 20px;
                margin-bottom: 5px;
            }
            .restaurant-item p {
                margin: 5px 0;
                font-size: 16px;
                color: #555;
            }
        </style>
    </head>
    <body>
        <header>🍽️ Here are your recommended Restaurants 🍽️</header>
        <div>
            <% if (recipes.length > 0) { %>
                <ul class="restaurant-list">
                    <% recipes.forEach(recipe => { %>
                        <li class="restaurant-item">
                            <h2><%= recipe.name %></h2>
                            <p><strong>Type:</strong> <%= recipe.cuisine || "Restaurant" %></p>
                            <p><strong>Address:</strong> <%= recipe.address %></p>
                            <p><strong>Rating:</strong> ⭐ <%= recipe.rating || "No rating available" %></p>     
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <p>No restaurant recommendations found.</p>
            <% } %>   
        </div> 
    </body>
</html>
<button class="back-btn" onclick="goBack()">🔙 Go Back</button>

<script>
    function goBack() {
        window.history.back();
    }
</script>

<style>
    .back-btn {
        display: block;
        margin: 20px auto;
        padding: 12px 20px;
        font-size: 18px;
        font-weight: bold;
        background-color: #ff4d4d; /* Red button */
        color: white;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }

    .back-btn:hover {
        background-color: #cc0000;
    }
</style>
